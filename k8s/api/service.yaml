apiVersion: v1
kind: Service
metadata:
  name: hackathon-api
  namespace: fiap-hackathon

spec:
  selector:
    app: hackathon-api

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP